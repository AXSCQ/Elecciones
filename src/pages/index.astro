---
import Layout from '../layouts/Layout.astro';
import Header from '../components/react/Header.jsx';
import { candidates } from '../data/candidates.js';
import Carousel from '../components/react/Carousel.jsx';
---

<Layout title="Elecciones Bolivia 2025 - Inicio">
  <Header client:load />
  
  <!-- Hero Section con animación de entrada -->
  <section class="relative min-h-[90vh] flex items-center justify-center overflow-hidden py-20 px-4">
    <div class="absolute inset-0 z-0 bg-gradient-to-b from-blue-900/40 to-black/70"></div>
    
    <div class="container mx-auto relative z-10">
      <div class="text-center mb-8 animate-fade-down animate-duration-1000">
        <h1 class="text-6xl md:text-7xl font-extrabold text-white mb-4 drop-shadow-[0_0_10px_rgba(0,0,0,0.8)]">
          <span class="text-yellow-400">Bolivia</span> Decide 2025
        </h1>
        <p class="text-xl md:text-2xl text-white/90 max-w-3xl mx-auto leading-relaxed">
          El futuro de Bolivia en tus manos. Conoce a quienes aspiran a liderar el país en las próximas elecciones presidenciales.
        </p>
      </div>
      
      <!-- Contador regresivo -->
      <div class="flex justify-center mb-12">
        <div class="grid grid-cols-2 md:grid-cols-4 gap-4 text-center">
          <div class="bg-black/30 backdrop-blur-sm p-4 rounded-lg border border-white/20">
            <span class="block text-4xl font-bold text-white">365</span>
            <span class="text-blue-200">Días</span>
          </div>
          <div class="bg-black/30 backdrop-blur-sm p-4 rounded-lg border border-white/20">
            <span class="block text-4xl font-bold text-white">24</span>
            <span class="text-blue-200">Horas</span>
          </div>
          <div class="bg-black/30 backdrop-blur-sm p-4 rounded-lg border border-white/20">
            <span class="block text-4xl font-bold text-white">60</span>
            <span class="text-blue-200">Minutos</span>
          </div>
          <div class="bg-black/30 backdrop-blur-sm p-4 rounded-lg border border-white/20">
            <span class="block text-4xl font-bold text-white">60</span>
            <span class="text-blue-200">Segundos</span>
          </div>
        </div>
      </div>
      
      <div class="flex justify-center">
        <a href="#candidatos" class="px-8 py-3 bg-yellow-500 text-black font-bold rounded-full hover:bg-yellow-400 transition-all duration-300 transform hover:scale-105 shadow-lg">
          Conoce a los Candidatos
        </a>
      </div>
    </div>
    
    <!-- Decoración de fondo -->
    <div class="absolute bottom-0 left-0 w-full h-24 bg-gradient-to-t from-black/80 to-transparent"></div>
  </section>

  <!-- Sección de Candidatos con efecto parallax -->
  <section id="candidatos" class="py-20 relative">
    <div class="absolute inset-0 z-0 bg-gradient-to-b from-black/80 via-blue-900/50 to-black/80"></div>
    
    <div class="container mx-auto relative z-10 px-4">
      <div class="text-center mb-16">
        <h2 class="text-5xl font-extrabold text-white mb-4 drop-shadow-lg inline-block relative">
          Candidatos Presidenciales
          <span class="absolute -bottom-2 left-0 w-full h-1 bg-yellow-400"></span>
        </h2>
        <p class="text-xl text-blue-200 max-w-2xl mx-auto">Conoce los perfiles, trayectorias y propuestas de los aspirantes a la presidencia.</p>
      </div>
      
      <!-- Carrusel mejorado -->
      <div class="w-full max-w-4xl mx-auto mb-16">
        <Carousel client:visible>
          {candidates.map(candidate => (
            <div class="group relative flex-shrink-0 w-80 h-[28rem] bg-black/40 backdrop-blur-sm rounded-2xl shadow-2xl border border-white/10 transition-all duration-500 hover:scale-105 hover:z-20 cursor-pointer overflow-hidden">
              <div class="absolute inset-0 z-0 pointer-events-none">
                <img 
                  src={candidate.image} 
                  alt={candidate.name}
                  class="transition-all duration-700 ease-out object-cover object-center object-top w-full h-full rounded-2xl opacity-80 blur-[1px] group-hover:blur-none group-hover:opacity-100 group-hover:scale-[1.15] group-hover:brightness-110"
                  style="will-change: transform, filter;"
                  loading="lazy"
                />
              </div>
              
              <!-- Overlay con gradiente -->
              <div class="absolute inset-0 bg-gradient-to-t from-black via-black/50 to-transparent opacity-70 group-hover:opacity-60 transition-opacity duration-500 rounded-2xl"></div>
              
              <!-- Contenido -->
              <div class="relative z-10 flex flex-col h-full justify-end p-6">
                <!-- Partido (Badge) -->
                <div class="absolute top-4 right-4">
                  <span class="px-3 py-1 bg-black/60 backdrop-blur-sm text-sm font-bold rounded-full border border-white/20" style={`color: ${candidate.partyColor}`}>
                    {candidate.partyShort}
                  </span>
                </div>
                
                <h2 class="text-3xl font-bold text-white mb-2 drop-shadow-lg transform group-hover:translate-y-0 transition-transform duration-500">{candidate.name}</h2>
                <p class="text-lg text-blue-200 font-semibold mb-4 drop-shadow-lg">{candidate.party}</p>
                
                <!-- Información adicional (visible en hover) -->
                <div class="transform translate-y-8 opacity-0 group-hover:translate-y-0 group-hover:opacity-100 transition-all duration-500">
                  <p class="text-white/80 text-sm mb-4 line-clamp-3">{candidate.bio.substring(0, 120)}...</p>
                  
                  <div class="flex gap-2">
                    <a href={`/candidato/${candidate.id}`} class="flex-1 px-4 py-2 bg-blue-600 text-white rounded-md font-bold shadow-md hover:bg-blue-700 transition-colors text-center">
                      Ver perfil
                    </a>
                    <a href={`/historia#${candidate.id}`} class="flex-1 px-4 py-2 bg-green-600 text-white rounded-md font-bold shadow-md hover:bg-green-700 transition-colors text-center">
                      Ver historia
                    </a>
                  </div>
                </div>
              </div>
              
              <!-- Efecto de brillo en hover -->
              <div class="absolute -inset-0.5 bg-gradient-to-r from-yellow-400 to-blue-600 rounded-2xl blur opacity-0 group-hover:opacity-30 transition duration-1000 group-hover:duration-200"></div>
            </div>
          ))}
        </Carousel>
      </div>
      
      <!-- Grid de candidatos (vista alternativa) -->
      <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-4 mb-8">
        {candidates.map(candidate => (
          <a href={`/candidato/${candidate.id}`} class="block group">
            <div class="relative overflow-hidden rounded-lg aspect-square bg-black/30 border border-white/10 transition-transform duration-300 group-hover:scale-105 group-hover:border-white/30">
              <img 
                src={candidate.image} 
                alt={candidate.name} 
                class="w-full h-full object-cover object-top opacity-80 group-hover:opacity-100 transition-opacity duration-300"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-black to-transparent"></div>
              <div class="absolute bottom-0 left-0 right-0 p-3 text-center">
                <h3 class="text-white font-bold text-lg truncate">{candidate.name}</h3>
                <p class="text-blue-200 text-sm truncate">{candidate.party}</p>
              </div>
            </div>
          </a>
        ))}
      </div>
      
      <div class="text-center">
        <a href="/candidatos" class="inline-block px-6 py-3 bg-blue-700 text-white rounded-md hover:bg-blue-800 transition-colors font-medium shadow-lg">
          Ver todos los candidatos
        </a>
      </div>
    </div>
  </section>

  <!-- Secciones adicionales -->
  <section class="py-16 relative">
    <div class="absolute inset-0 z-0 bg-gradient-to-b from-black/80 to-blue-900/30"></div>
    
    <div class="container mx-auto relative z-10 px-4">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <!-- Partidos -->
        <div class="bg-gradient-to-br from-purple-800/50 to-blue-800/50 rounded-xl p-8 shadow-lg border border-white/10 flex flex-col items-center transform transition-transform duration-500 hover:scale-105 hover:shadow-2xl">
          <div class="w-20 h-20 rounded-full bg-purple-600/30 flex items-center justify-center mb-4">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
            </svg>
          </div>
          <h2 class="text-2xl font-bold text-white mb-2">Partidos Políticos</h2>
          <p class="text-gray-300 mb-6 text-center">Descubre la historia, ideología y alianzas de los partidos que compiten en estas elecciones.</p>
          <a href="/partidos" class="mt-auto px-5 py-2 bg-purple-700 text-white rounded-md hover:bg-purple-600 transition-colors font-medium w-full text-center">
            Explorar Partidos
          </a>
        </div>
        
        <!-- Historia Electoral -->
        <div class="bg-gradient-to-br from-green-800/50 to-blue-800/50 rounded-xl p-8 shadow-lg border border-white/10 flex flex-col items-center transform transition-transform duration-500 hover:scale-105 hover:shadow-2xl">
          <div class="w-20 h-20 rounded-full bg-green-600/30 flex items-center justify-center mb-4">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <h2 class="text-2xl font-bold text-white mb-2">Historia Electoral</h2>
          <p class="text-gray-300 mb-6 text-center">Explora los hitos y eventos clave que han definido la democracia boliviana a lo largo del tiempo.</p>
          <a href="/historia" class="mt-auto px-5 py-2 bg-green-700 text-white rounded-md hover:bg-green-600 transition-colors font-medium w-full text-center">
            Ver Cronología
          </a>
        </div>
        
        <!-- Sabías que -->
        <div class="bg-gradient-to-br from-yellow-500/40 to-purple-800/30 rounded-xl p-8 shadow-lg border border-white/10 flex flex-col items-center transform transition-transform duration-500 hover:scale-105 hover:shadow-2xl">
          <div class="w-20 h-20 rounded-full bg-yellow-500/30 flex items-center justify-center mb-4">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z" />
            </svg>
          </div>
          <h2 class="text-2xl font-bold text-white mb-2">¿Sabías que?</h2>
          <p class="text-gray-300 mb-6 text-center">Descubre datos curiosos y sorprendentes sobre la política y las elecciones en Bolivia.</p>
          <a href="/sabiasque" class="mt-auto px-5 py-2 bg-yellow-500 text-black font-medium rounded-md hover:bg-yellow-400 transition-colors w-full text-center">
            Descubrir Más
          </a>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style is:global>
/* Scrollbar estilizado para el carrusel */
::-webkit-scrollbar {
  height: 10px;
  background: #223366;
  border-radius: 6px;
}
::-webkit-scrollbar-thumb {
  background: #3045b5;
  border-radius: 6px;
}

/* Animaciones adicionales */
@keyframes float {
  0% { transform: translateY(0px); }
  50% { transform: translateY(-10px); }
  100% { transform: translateY(0px); }
}

.animate-float {
  animation: float 3s ease-in-out infinite;
}

@keyframes fade-down {
  0% {
    opacity: 0;
    transform: translateY(-20px);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}

.animate-fade-down {
  animation: fade-down 1s ease-out forwards;
}

.animate-duration-1000 {
  animation-duration: 1000ms;
}
</style>
